# ocaz

```sh
cd ~/repo/github.com/kleamp1e/ocaz/
docker-compose build

docker-compose -f docker-compose.yaml -f docker-compose.dev.yaml run --rm ocaz
# pytest
# bin/add.py --data-dir data --log-level debug URL
# bin/detect_face.py --data-dir data --log-level OBJECT_ID
```
